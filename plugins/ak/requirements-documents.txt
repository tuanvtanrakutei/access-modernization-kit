# Optional: local readers for document evidence.
#
# Only needed when Phase 5 document integration has to read spreadsheets, PDFs, Word,
# or PowerPoint locally. When the agent runtime provides its own document readers, or
# when Graphify normalizes the corpus, these are not required - `preflight` recommends
# them only for the formats a manifest actually declares.
#
#   pip install -r plugins/ak/requirements-documents.txt
openpyxl>=3.1.5,<4
pypdf>=6.0.0,<7
python-docx>=1.2.0,<2
xlrd>=2.0.2,<3
python-pptx>=1.0.2,<2
PyMuPDF>=1.26.3,<2
