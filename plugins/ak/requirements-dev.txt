# Development requirements: everything a user needs, plus the test runner.
#
# Contributors run this. Users do not - it pulls in pytest, which nothing in the
# package's own commands imports. See requirements.txt for the runtime minimum.
#
#   pip install -r plugins/ak/requirements-dev.txt
-r requirements.txt
-r requirements-documents.txt
pytest>=9.1.1,<10
